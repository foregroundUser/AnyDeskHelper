<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AnyDeskHelper" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/primary0</item>
        <item name="android:navigationBarColor">@color/primary0</item>
        <item name="android:statusBarColor">@color/primary0</item>

        <item name="colorSurface">@color/primary0</item>
        <item name="android:colorBackground">@color/primary0</item>
        <item name="colorOnBackground">@color/text_primary</item>
    </style>
</resources>